import React from 'react';

import { SPAN } from './styles';

const TextErrorMessage: React.FC = ({ children }) => (
    
    <SPAN>
        { children }
    </SPAN>
);

export default TextErrorMessage;